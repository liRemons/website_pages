# 模块: src\apps\tableConfig\components\ConfigBuilder

### src\apps\tableConfig\components\ConfigBuilder

> **模块用途**: 页面模块，主要负责 UI 展示和路由

- **文件数**: 3
- **总行数**: 1212
- **文件类型**: .jsx: 3

**函数说明**:
- `ConfigBuilder` — * 配置构建器主组件（index.jsx）
- `fileInputRef` — * 配置构建器主组件（index.jsx）
- `updateConfig` — * 配置构建器主组件（index.jsx）
- `handleExport` — * 配置构建器主组件（index.jsx）
- `json` — * 配置构建器主组件（index.jsx）
- `handleImport` — * 配置构建器主组件（index.jsx）
- `confirmImport` — * 配置构建器主组件（index.jsx）
- `importedConfig` — * 配置构建器主组件（index.jsx）
- `handleFileImport` — * 配置构建器主组件（index.jsx）
- `importedConfig` — * 配置构建器主组件（index.jsx）
- `loadExample` — * 配置构建器主组件（index.jsx）
- `exampleConfig` — * 配置构建器主组件（index.jsx）
- `clearConfig` — * 配置构建器主组件（index.jsx）
- `copyToClipboard` — * 配置构建器主组件（index.jsx）
- `downloadConfig` — * 配置构建器主组件（index.jsx）
- `handlePreview` — * 配置构建器主组件（index.jsx）
- `validation` — * 配置构建器主组件（index.jsx）
- `SearchConfig` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleAdd` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleDelete` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleMove` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleEdit` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleSave` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleCancel` — * 搜索条件配置组件（SearchConfig.jsx）
- `openCodeEditor` — * 搜索条件配置组件（SearchConfig.jsx）
- `handleCodeEditorOk` — * 搜索条件配置组件（SearchConfig.jsx）
- `dataSourceType` — * 搜索条件配置组件（SearchConfig.jsx）
- `options` — * 搜索条件配置组件（SearchConfig.jsx）
- `TableConfig` — * 表格列配置组件（TableConfig.jsx）
- `handleAdd` — * 表格列配置组件（TableConfig.jsx）

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\tableConfig\components\ConfigBuilder\index.jsx | 448 | - |
| src\apps\tableConfig\components\ConfigBuilder\SearchConfig.jsx | 411 | - |
| src\apps\tableConfig\components\ConfigBuilder\TableConfig.jsx | 353 | - |

