# 模块: src\apps\travelBadge\components\templatePanel

### src\apps\travelBadge\components\templatePanel

> **模块用途**: 页面模块，主要负责 UI 展示和路由

- **文件数**: 2
- **总行数**: 949
- **文件类型**: .jsx: 1, .less: 1

**函数说明**:
- `TemplateCard` — * 模板卡片（index.jsx）
- `frameColor` — * 模板卡片（index.jsx）
- `InlineEditForm` — * 模板卡片（index.jsx）
- `handleConfirm` — * 模板卡片（index.jsx）
- `TemplatePanel` — * 模板卡片（index.jsx）
- `safeApplyTemplate` — * 模板卡片（index.jsx）
- `handleCustomEdit` — * 模板卡片（index.jsx）
- `handleCustomEditConfirm` — * 模板卡片（index.jsx）
- `handleSystemEdit` — * 模板卡片（index.jsx）
- `handleSystemEditConfirm` — * 模板卡片（index.jsx）
- `handleSyncCanvas` — * 模板卡片（index.jsx）
- `handleAddConfirm` — * 模板卡片（index.jsx）

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\travelBadge\components\templatePanel\index.jsx | 523 | - |
| src\apps\travelBadge\components\templatePanel\index.less | 426 | - |

