# 模块: src\apps\travelBadge\components\canvasArea

### src\apps\travelBadge\components\canvasArea

> **模块用途**: 页面模块，主要负责 UI 展示和路由

- **文件数**: 2
- **总行数**: 333
- **文件类型**: .jsx: 1, .less: 1

**函数说明**:
- `GuideLineLayer` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）
- `CanvasArea` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）
- `replaceImageInputRef` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）
- `replacingElementIdRef` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）
- `handleReplaceImage` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）
- `handleReplaceImageFileChange` — * 标线层：渲染拖拽对齐时的参考线（index.jsx）

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\travelBadge\components\canvasArea\index.jsx | 201 | - |
| src\apps\travelBadge\components\canvasArea\index.less | 132 | - |

