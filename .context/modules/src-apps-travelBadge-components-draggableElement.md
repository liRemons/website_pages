# 模块: src\apps\travelBadge\components\draggableElement

### src\apps\travelBadge\components\draggableElement

> **模块用途**: 页面模块，主要负责 UI 展示和路由

- **文件数**: 2
- **总行数**: 611
- **文件类型**: .jsx: 1, .less: 1

**函数说明**:
- `getRotatedCursor` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `DraggableElement` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `elementRef` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `textareaRef` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleMouseDown` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onMouseMove` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onMouseUp` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleTouchStart` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onTouchMove` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onTouchEnd` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `applyResizeDelta` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleResizeMouseDown` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onMouseMove` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onMouseUp` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleResizeTouchStart` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onTouchMove` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `onTouchEnd` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleMenuClick` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleDoubleClick` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleEditBlur` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）
- `handleEditKeyDown` — * 根据元素旋转角度，计算 handle 对应的 cursor 方向（index.jsx）

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\travelBadge\components\draggableElement\index.jsx | 466 | - |
| src\apps\travelBadge\components\draggableElement\index.less | 145 | - |

