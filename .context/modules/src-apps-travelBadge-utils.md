# 模块: src\apps\travelBadge\utils

### src\apps\travelBadge\utils

> **模块用途**: 工具函数模块，提供通用辅助方法

- **文件数**: 6
- **总行数**: 1029
- **文件类型**: .js: 6

**函数说明**:
- `createChangeZIndex` — * 手动调整层级：up=上移一层, down=下移一层, top=置顶, bottom=置底（canvasHelpers.js）
- `createAddImageElement` — * 手动调整层级：up=上移一层, down=下移一层, top=置顶, bottom=置底（canvasHelpers.js）
- `createAddTextElement` — * 手动调整层级：up=上移一层, down=下移一层, top=置顶, bottom=置底（canvasHelpers.js）
- `hexToRgba` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `r` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `g` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `b` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `loadImage` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `calcObjectFitRect` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `drawImageElement` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `measureLineWidth` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `drawLineWithSpacing` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `drawTextElement` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `lineWidth` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `drawFrame` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `borderWidth` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `exportToImage` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `captureCanvasCover` — * 原生 Canvas API 导出图片（exportCanvas.js）
- `loadFont` — * 远程字体加载工具（fontLoader.js）
- `loadRemoteFonts` — * 远程字体加载工具（fontLoader.js）
- `isFontLoaded` — * 远程字体加载工具（fontLoader.js）
- `computeSnapAndGuideLines` — * 吸附与对齐辅助线计算（snapHelpers.js）
- `generateId` — * 样式工具函数（styleHelpers.js）
- `buildTextShadow` — * 样式工具函数（styleHelpers.js）
- `buildWebkitStroke` — * 样式工具函数（styleHelpers.js）
- `buildTextColor` — * 样式工具函数（styleHelpers.js）
- `makeSelectToken` — * 样式工具函数（styleHelpers.js）
- `makeInputNumberToken` — * 样式工具函数（styleHelpers.js）
- `makeInputToken` — * 样式工具函数（styleHelpers.js）

**主要导出**:
- src\apps\travelBadge\utils\canvasHelpers.js:createChangeZIndex
- src\apps\travelBadge\utils\canvasHelpers.js:createAddImageElement
- src\apps\travelBadge\utils\canvasHelpers.js:createAddTextElement
- src\apps\travelBadge\utils\constants.js:FONT_FAMILIES
- src\apps\travelBadge\utils\constants.js:FRAME_TEMPLATES
- src\apps\travelBadge\utils\constants.js:MOCK_IMAGES
- src\apps\travelBadge\utils\constants.js:DEFAULT_TEXT_PROPS
- src\apps\travelBadge\utils\exportCanvas.js:exportToImage
- src\apps\travelBadge\utils\exportCanvas.js:captureCanvasCover
- src\apps\travelBadge\utils\fontLoader.js:REMOTE_FONTS
- src\apps\travelBadge\utils\fontLoader.js:loadFont
- src\apps\travelBadge\utils\fontLoader.js:loadRemoteFonts
- src\apps\travelBadge\utils\fontLoader.js:isFontLoaded
- src\apps\travelBadge\utils\snapHelpers.js:computeSnapAndGuideLines
- src\apps\travelBadge\utils\styleHelpers.js:generateId
- src\apps\travelBadge\utils\styleHelpers.js:buildTextShadow
- src\apps\travelBadge\utils\styleHelpers.js:buildWebkitStroke
- src\apps\travelBadge\utils\styleHelpers.js:buildTextColor
- src\apps\travelBadge\utils\styleHelpers.js:makeSelectToken
- src\apps\travelBadge\utils\styleHelpers.js:makeInputNumberToken

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\travelBadge\utils\canvasHelpers.js | 144 | createChangeZIndex, createAddImageElement, createAddTextElement |
| src\apps\travelBadge\utils\constants.js | 300 | FONT_FAMILIES, FRAME_TEMPLATES, MOCK_IMAGES |
| src\apps\travelBadge\utils\exportCanvas.js | 299 | exportToImage, captureCanvasCover |
| src\apps\travelBadge\utils\fontLoader.js | 111 | REMOTE_FONTS, loadFont, loadRemoteFonts |
| src\apps\travelBadge\utils\snapHelpers.js | 101 | computeSnapAndGuideLines |
| src\apps\travelBadge\utils\styleHelpers.js | 74 | generateId, buildTextShadow, buildWebkitStroke |

