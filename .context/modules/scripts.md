# 模块: scripts

### scripts

- **文件数**: 4
- **总行数**: 659
- **文件类型**: .js: 4

**函数说明**:
- `redrawAllLines` — * TTY 模式下整块重绘所有进度行。（build.js）
- `pageDistDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `pageBackupDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `backupPageDist` — * TTY 模式下整块重绘所有进度行。（build.js）
- `distDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `bakDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `removePageBackup` — * TTY 模式下整块重绘所有进度行。（build.js）
- `bakDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `restorePageDist` — * TTY 模式下整块重绘所有进度行。（build.js）
- `distDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `bakDir` — * TTY 模式下整块重绘所有进度行。（build.js）
- `cleanLegacyDistDirs` — * TTY 模式下整块重绘所有进度行。（build.js）
- `PAGE_BUILD_TIMEOUT_MS` — * TTY 模式下整块重绘所有进度行。（build.js）
- `buildPage` — * TTY 模式下整块重绘所有进度行。（build.js）
- `child` — * TTY 模式下整块重绘所有进度行。（build.js）
- `timeoutTimer` — * TTY 模式下整块重绘所有进度行。（build.js）
- `runWithConcurrency` — * TTY 模式下整块重绘所有进度行。（build.js）
- `startNext` — * TTY 模式下整块重绘所有进度行。（build.js）

| 文件 | 行数 | 导出 |
|------|------|------|
| scripts\build.js | 353 | - |
| scripts\common.js | 257 | - |
| scripts\dev.js | 8 | - |
| scripts\log.js | 41 | - |

