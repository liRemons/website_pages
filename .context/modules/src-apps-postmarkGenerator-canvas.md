# 模块: src\apps\postmarkGenerator\canvas

### src\apps\postmarkGenerator\canvas

- **文件数**: 1
- **总行数**: 525
- **文件类型**: .js: 1

**函数说明**:
- `degToRad` — 角度（度）→ 弧度（drawers.js）
- `applyWornEffect` — 角度（度）→ 弧度（drawers.js）
- `drawArcText` — 角度（度）→ 弧度（drawers.js）
- `letterSpacingAngle` — 角度（度）→ 弧度（drawers.js）
- `drawClassic` — 角度（度）→ 弧度（drawers.js）
- `drawArc` — 角度（度）→ 弧度（drawers.js）
- `drawVintage` — 角度（度）→ 弧度（drawers.js）
- `drawArcStamp` — 角度（度）→ 弧度（drawers.js）
- `arcUpStart` — 角度（度）→ 弧度（drawers.js）
- `arcUpEnd` — 角度（度）→ 弧度（drawers.js）
- `arcDnStart` — 角度（度）→ 弧度（drawers.js）
- `arcDnEnd` — 角度（度）→ 弧度（drawers.js）
- `drawApproval` — 角度（度）→ 弧度（drawers.js）
- `drawStar` — 角度（度）→ 弧度（drawers.js）
- `drawPostal` — 角度（度）→ 弧度（drawers.js）
- `SPACING_RAD` — 角度（度）→ 弧度（drawers.js）
- `SPAN_RAD` — 角度（度）→ 弧度（drawers.js）
- `calcArcFontSize` — 角度（度）→ 弧度（drawers.js）
- `locParams` — 角度（度）→ 弧度（drawers.js）
- `subParams` — 角度（度）→ 弧度（drawers.js）
- `drawStampToCanvas` — 角度（度）→ 弧度（drawers.js）

**主要导出**:
- src\apps\postmarkGenerator\canvas\drawers.js:degToRad
- src\apps\postmarkGenerator\canvas\drawers.js:drawStampToCanvas

| 文件 | 行数 | 导出 |
|------|------|------|
| src\apps\postmarkGenerator\canvas\drawers.js | 525 | degToRad, drawStampToCanvas |

